# Development dependencies for the Aladdin Spec reference validator.
#
# These tools run in the source repository and in CI only. They are never
# part of a published Knowledge Pack artifact (ADR-0003).

jsonschema[format]>=4.23,<5
PyYAML>=6.0,<7
pytest>=8.0,<10
